export type LocalePagesErrors = {
  accessDenied: { title: string; subtitle: string };
  internalServer: { title: string; subtitle: string };
  notFound: { title: string; subtitle: string };
  shared: {
    returnToHome: string;
  };
};
